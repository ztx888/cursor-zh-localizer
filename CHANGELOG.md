# Changelog

## 1.0.2 - 2026-05-26

- 修复帮助菜单里带 macOS 助记符的 `Give Feedback...` 和 `Open Process Explorer` 残留英文。
- 将编辑菜单里的 `Emmet：展开缩写` 优化为更自然的 `展开 Emmet 缩写`。
- 补齐模型参数菜单里的扩展思考、思考强度和上下文标题及说明。
- 扩展用户配置修正逻辑，覆盖运行时模型缓存中的 `thinking`、`effort` 和 `context` 参数。

## 1.0.1 - 2026-05-26

- 补齐 macOS 顶部菜单中的布局菜单项：辅助侧边栏、面板、面板位置、面板对齐等。
- 补齐顶部菜单中的缩放项：放大、缩小、设置缩放级别、重置缩放。
- 补齐 `Emmet：展开缩写` 和 `关闭窗口` 等原生菜单文案。
- 同步补充对应命令注册标题，减少菜单和命令面板来源不一致导致的漏翻。

## 1.0.0 - 2026-05-26

- 首次公开版本。
- 覆盖 Cursor 设置页、Agent 面板、模型设置、审查页、工具调用状态和常见悬浮提示。
- 支持重复运行，适合 Cursor 更新后重新打补丁。
- 支持 `--backup`、`--app`、`--app-root` 和 `--no-user-storage`。
- 自动更新 Cursor `product.json` 校验和，减少补丁后校验异常。
